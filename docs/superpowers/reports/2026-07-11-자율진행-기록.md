# 자율진행 기록 — 2026-07-11 (사용자 부재 중 자가발전루프)

> 사용자 지시: "도커 등 실증테스트는 묻지 말고 진행, 로드맵 계속, 기록파일로 남겨줘. 자리 비움. 자가발전루프."
> 이 파일이 그 기록이다. 작업단위가 끝날 때마다 여기와 PROGRESS.md를 갱신한다.

## ✅ 완료·커밋 (거짓양호 감사 F1~F5 + UI, 6커밋)

| 커밋 | 내용 | 테스트 |
|---|---|---|
| `4535445` | **F2** DBM-025 EoS det_common 제거 → judge_eol 권위경로(노후 rules='12' 거짓양호 차단), DET_SOURCE STUB 이중방어 | 2006 |
| `8245cf8` | **F5** DBM-013 cloud 와일드카드 parity — rds/aurora/azure `10.%`/`%.corp.com` 거짓양호 봉쇄(R-MY013/R-MA013-CLOUD) | 2030 |
| `a8e6734` | **④ 파일명 인지 Fable 리뷰 → SHIP** + L-1 variant_from_filename NFC 정합 | 23/23 |
| `8359650` | **웹UI** 판정근거=xlsx 판단기준 표시(Judgment.standard 전파→JSON→상세패널) + 다중업로드 진행표시·파일별 즉시반영 | 2044 |
| `b635a5f` | **F1** DBM-008 수집키 정합(R-MY008/R-OR008) — mysql `_1` 추가, oracle `_2` UNLIMITED 복원(+resource_name 구조적 배제) | +11 |
| `d6b8ab0` | **모드J** fail-closed 미수집 가드(008/009/013/014) + **F3/F4** + oracle cov 픽스처 2건 교체 | **2077 passed / 100 skipped / 0 failed** |

- F1 발견: oracle 옛 주석코드에 resource_name 체크가 원래 없었음 → 스펙대로 구조적 배제 명시(우연 통과 제거).
- F5 발견: oracle_rds DBM-013=ABSENT(대상 아님), mariadb cloud는 mariadb_rds만 배선됨.

## ✅ Opus 통합 리뷰 결과 — **SHIP** (수정사항 없음)

6커밋 전부 통과. 모드J 위반0-only 발동, 모드D/D3와 교집합 0, F1 KeyError 다층방어, F5 parity 완전 일치, F2 라우팅, UI escapeHtml XSS 안전 — 모두 실증 확인. 발견 Low 3건(비블로킹, 백로그 등재):
- (L1) mariadb/mssql native DBM-008 수집키 접미사 여부 미검증 — 수집기 확인 필요(팀 인지).
- (L2) 모드J DBM-008 checker=None이라 부분수집(_1 있고 _2 누락) 미포착 — 엔진별 기대행 checker 확장 검토.
- (L3) F5 리터럴 `_` 호스트 과탐 — MySQL 와일드카드 의미론상 정당, 안전방향 의도 고정.

## ✅ F8 완료·커밋 (`363911e`, 2092 passed / 0 failed)

container 명령실패 출력(→N→양호) 정규식 가드. 핵심 판단: 스펙 예시 `Error from server`/`Unauthorized`를
그대로 넣으면 **PRCC-013 익명접근 정상차단 양호증거(`Error from server (Forbidden)`)를 과트리거** →
의도적으로 제외하고 `^error:` 줄시작 앵커 등 좁은 패턴 채택, 대조군 검증 완료.
실증: 도커 오류 5종 실측 매치 + collected 실샘플 37항목 전/후 diff 0. kubectl은 로컬 미보유로 문헌 기반(주석 명기).

## ▶ 로드맵 진행: 컨테이너 §3 착수 (2026-07-11)

- §3-1 normalize_id PRC-C→PRCC: **이미 완료돼 있음 확인**(profile.py 구현+회귀테스트 존재) — 스킵.
- §3-2 kind 클러스터 실증: **✅ 완료(코드 무변경, 보고 전용)** — kind 0.32.0/kubectl 1.36.2 brew 설치,
  prcc-lab 클러스터 + docker:dind로 실 수집 스크립트 원본 무수정 실행(39/39·40/40 체크), XML 2종 확보(`out/kind_lab/`).
  - (a) id 실표기 `PRC-C-NNN` → normalize_id 정합 ✅ (§3-1 갭은 이미 닫혀 있었음 재확인)
  - (b) 실XML엔 `<asset><app>k8s_master</app>` 단일 필드 — 파서 `<app>` 폴백이 정확히 이 케이스 사전 대응, k8s_master/docker_linux 식별 ✅
  - (c) 마스킹: raw XML엔 kubeconfig PEM 평문 존재하나 파서 `_mask_container_evidence`가 전량 REDACTED — **evidence 갭 0건** ✅
  - judge 스모크: k8s_master 35항목(양호23/취약7/보류5), docker 32항목(17/13/2), 크래시 0.
    **F8 가드 과트리거 0** (독립 신규 샘플 재확인). 판단보류 원인은 qwen3-coder:30b 미기동(HTTPError, 환경).
  - 정리 완료: 클러스터·dind 삭제, 산출물 out/kind_lab*만.
- §3-3 cov 계약: **✅ 완료·커밋(`59a8ef7`, 2293 passed)** — k8s_master DET 33 + docker_linux DET 31
  양극성 전수(+169 tests), MANUAL/ABSENT 36조합 hold 계약, 미커버 1극성(PRCC-017/k8s/vuln, 구조적 no-op) 사유 명시.
  **라벨 재분류 불요**(현행 det_common+label A 구조가 gate 동적 라우팅으로 정확).
- §3-3 발견 벤더버그 → **수정 배치 진행 중**:
  - **High 거짓양호 PRCC-045(k8s_master)**: 코드 마커 `spec.hostUTS` vs 실스크립트 `.securityContext.hostUTS`
    불일치 + vanilla k8s에 hostUTS 필드 부재 → 항상 양호. 설계=fail-closed(DET→MANUAL 강등 검토).
  - Medium 거짓취약 5항목(PRCC-023/035/036/037/038 docker): 마커 `[does not exist]` vs 실측 `[not exist]` → 항상 취약. 마커 추가 인식.
  - Low PRCC-031 dead-code 조건: 과탐(안전)방향 — 수정 안 함, 보고만.

## ✅ F6+F7 완료·커밋 (`b72a2be`, 2125 passed / 0 failed)

- 모드J에 DBM-005(mssql 한정)/006(4엔진)/007(mysql·oracle·mssql) checker 등록. pg 모드B 선행 확인(불변 실증).
- empty→양호 픽스처 **6곳** 교체(스펙은 4곳이라 했으나 실제 6곳 — 스펙-코드 불일치 보고됨).
- **발견 OBS-OR007**: oracle DBM-007 config 예외/규칙이 빈 리스트 → 수집행 무조건 위반(거짓취약 방향).
  good_verdict를 도달가능한 진실(판단보류)로 정정 + 회귀핀. 벤더 수정은 백로그.
- **발견 OBS-MY006**: mysql DBM-006 native(USER_ATTRIBUTES) vs cloud(FAILED_LOGIN_ATTEMPTS) 필드 상이 — checker 양쪽 수용.
- mariadb DBM-007 checker 의도적 미등록(R3 흡수 벤더버그 스코프 밖).
- 도커 실증: mysql:8/mariadb:11/postgres:16 실쿼리로 "0행 비현실" 전제 검증. oracle/mssql 이미지는
  **디스크 부족(I/O error)으로 pull 실패** → 실수집 collected 데이터로 대체(보고됨).
- 실수집 5엔진×3항목=15조합 judge() 회귀: verdict 변화 0.

## ▶ §1 서버 도커 자가수집 실증 완료 (코드 무변경, 보고)

- ubuntu:24.04 + fsi_unix.sh 원본 무수정 실행(root) → 실 XML good/vuln 쌍(`out/srv_lab/`), 67/70 항목 수집.
- (a) M2: detect_variant→linux 정확, 70/70 판정 무크래시. (b) crypt 해시 마스킹 정상 작동(yescrypt→REDACTED),
  **L2 갭 실증**: `DB_PASSWORD=...`/`api_key=...` 평문 통과(server_xml.py:88-128, 일반 시크릿 패턴 부재).
  (c) label C/D canned 보류 설계대로. LLM 실패(HTTPError) 13건 전부 판단보류 폴백 — LLM 장애발 거짓양호 0.
- 양극성 실플립 확인: SRV-026(PermitRootLogin)/SRV-028 — good→양호, sshd 기동+취약설정→취약(근거 정확).
- 취약 판정 6~8건 전수 근거 점검 — 거짓양호 후보 0.
- 후속 권고: 픽스처 승격용으론 단일변수 재수집이 깔끔(현 vuln 샘플은 다변수 혼합).

## ▶ §2 웹WAS 도커 자가수집 실증 완료 (코드 무변경, 보고)

- tomcat:10-jre17 실컨테이너에서 fsi_unix.sh 원본 무수정 실행, **첫 실 tomcat 샘플 good/vuln 쌍 확보**(`out/was_lab/`).
- **🚨 WST-033 실 거짓양호 버그 발견**: `check_WST_033`의 서비스 체크가 `output_arr[0]`(진단 브래킷 헤더)에
  매칭 — 헤더가 검색 패턴과 같은 토큰(`apache` 등)을 항상 포함 → "미기동→양호" 조기반환이 도달불가 죽은코드
  → Apache 부재 호스트에서 "버전 2.1 이상 탐지" **허위 근거**로 양호. 기존 합성 픽스처 57건은 전부 실프로세스
  라인 포함이라 이 형상 미커버. → §1 완료 후 수정 배치.
- **🚨 마스킹 갭 실증(L2)**: `_mask_server_evidence` 3패턴(PEM/crypt/hex)뿐 —
  `password="..."`(tomcat-users.xml)·`keystorePass=`/`certificateKeystorePassword=`(server.xml)가
  마스킹 후 evidence(LLM 투입 필드)에 평문 잔존. → 수정 배치.
- 사실 확정: nginx는 변형·파서·스크립트 전부 부재(완전 미지원, 작업 불요). unix 스크립트는 WST 항목 중
  6개만 실제 수집(WST-033/034/035/038/044/102) — 커버리지 갭. tomcat/jeus WST는 의도된 ABSENT(기존 회귀 커버).
- judge 스모크: 85/85 무크래시(판단보류 84 / 양호 1=위 버그).

## ✅ L2 마스킹 + R-WST033 수정 커밋 (`0cc3239`, 2306 passed / 0 failed)

- L2: 속성형(password=/keystorePass= 등)·셸KV형(PASSWORD/API_KEY/TOKEN 계열) 시크릿 마스킹 추가.
  실측 XML 전/후 diff로 타깃값만 치환·판정 라인(PASS_MAX_DAYS/PAM/PermitRootLogin) 무손상 검증.
  raw_evidence(결정론용) 불변 — evidence(LLM 투입)만.
- R-WST033: 브리프의 헤더매칭 결함에 더해 **2차 결함(이스케이프된 `\|` = 가짜 alternation)** 발견·동시 수정
  (하나만 고치면 실 Apache 케이스 전체 회귀였음 — 테스트로 포착). 버전 미검출 시 허위 "2.1 이상 탐지" 양호
  → `(*)` 마커→Low-1 가드→판단보류. 형제 체크 감사: 동일 결함 없음(사실 확인).
- **2차 Opus 리뷰 결과: SHIP** (d6b8ab0..HEAD 6커밋) — 거짓양호 신규 유입 0, 전 가드 fail-closed 실증
  (F8 가드가 실제 kind 샘플의 `Error response from daemon` 케이스를 판단보류로 정확 차단하는 것까지 확인),
  모드J checker 5엔진 실수집 필드 정합(거짓보류 폭주 위험 0), L2 마스킹-결정론 분리 계약(raw_evidence) 코드로 확정,
  WST-033 실샘플 신/구 비교(구=거짓양호→신=판단보류). 발견 3건 전부 저심각: ①F8 주석 문구(→`69e7397`로 정정),
  ②광범위 토큰 과보류 방향(허용), ③L2 패턴B `\S+` 표시 전용 과치환(저심각, 백로그).
- ✅ 서버 cov 실포맷 승격 커밋(`00bb4a1`, 2307 passed / 100 skipped): 단일변수 도커 재수집 3회
  (goodbase/vuln026/vuln028, verdict 플립 실확인) → SRV-026/028 실측 CDATA 승격.
  **SRV-007 검증공백 발견·해소**: 합성 good 픽스처가 split_output(4) 요구 미달로 분기 미실행인 채
  skip 통과해 온 갭 — 실측 3-delimiter 구조로 정정해 실제 분기 검증화.
- ✅ §4 OS가상화 선행 커밋(`6a728a8`): vpxuser/SAML어서션/SSO Bearer 마스킹(공유 마스커, 과마스킹 가드 +12핀),
  **osvirt 라벨분류 제안 문서**(`2026-07-11-osvirt-label-proposal.md`) — 35항목 중 3건 변경 제안
  (PRCV-003/022/023, **사용자 승인 대기**, yaml 무변경). 패치성 D 후보는 시트에 부재(스펙 힌트와 다른 사실 확인).
- ✅ §5 네트워크 수집기 초안 커밋(`5d4cb5a`): `scripts/fsec_network_collect.sh`(cisco read-only 7명령 화이트리스트,
  실패내성, --mask 1차) + `scripts/net_collect_mapping.md`(45항목 매핑표) + `scripts/net_manual_to_xml.py`(수기 변환기).
  mock으로 실파서 라운드트립 + judge 45/45 스모크 무크래시. **현장 협의 필요 잔여 6건**(IOS pagination,
  Invalid input 휴리스틱, CSV host 의미론, 비cisco 9벤더, --mask 범위, NET-012 실측 검증)은 스크립트/매핑표에 명시.

## 🏁 자율루프 종료 (모든 자율 가능 항목 소진)

**최종: 19커밋, 2005 → 2335 passed / 100 skipped / 0 failed. Opus 리뷰 2회 모두 SHIP.**

거짓양호 방향 실수정 총계: F1(DBM-008 수집키)·F2(DBM-025 EoS)·F3/F4/F6/F7(모드J 0행·기대행 가드)·
F5(cloud 와일드카드)·F8(container 오류출력)·PRCC-045(hostUTS)·R-WST033(Apache 헤더매칭)·
L2+속성형 마스킹·vpxuser/SSO 마스킹. 거짓취약 수정: R-PRCC-NOTEXIST(5항목).

**남은 것 전부 사용자 게이트**:
1. push 여부(로컬 19커밋 대기) 2. osvirt 라벨 3건 제안 승인 3. FW B′-2(ref/FW 실데이터 환경 필요)
4. qwen3-coder:30b 설치(디스크 확보 선행) 5. 네트워크 수집기 현장 협의 6건 6. 백로그(OBS-OR007,
mariadb/mssql DBM-008 수집키 확인, L2 패턴B 다듬기)
- 웹UI 시연 서버는 127.0.0.1:8766에 켜둔 상태(시연-2026-07-11 프로젝트에서 결과 확인 가능).
  스크린샷은 세션 scratchpad에 있음. 8765의 구버전 서버는 손대지 않았음.

## ⚠️ 사용자 확인 필요 (부재 중 발견)

- **호스트 디스크 3.1GB 남음(79%)** — oracle/mssql 도커 실증이 이것 때문에 막혔음. 오늘 실증용으로
  받은 이미지(mysql/mariadb/postgres/dind)는 정리했으나 Docker.raw 자체 축소는 Docker Desktop에서
  수동 필요할 수 있음. 디스크 확보 권장.
- Opus 리뷰 Low-1/L-2 + OBS-OR007/OBS-MY006 → 백로그 승격 여부.

## 🔄 진행 중 (백그라운드)

1. ~~F6+F7 배치~~ ✅ 위 완료 (Sonnet) — DBM-006/007·DBM-005(mssql_rds) 0행→양호 수정 + **도커 실증**(f6dbm_* 컨테이너, 사용자 사전승인) + cov 픽스처 empty→양호 4곳 교체.
3. **F8** (Sonnet) — container 오류출력→N→양호 정규식 가드, collected/container 실샘플 과트리거 0 검증.
4. **웹UI Playwright 시연** — 다중업로드 3파일 ✅(스크린샷 scratchpad 01~03), mysql 판정 실행 중(qwen3.5, production 모델 qwen3-coder:30b 미설치) → 판단기준(xlsx) 표시 스크린샷 예정.

## 📋 다음 로드맵 (진행 순서)

1. F6/F7/F8 랜딩 → 커밋 → Opus 리뷰 반영 → PROGRESS.md 갱신.
2. **컨테이너 normalize_id+kind** (잔여도메인 설계 spec 참조) — F8 랜딩 후 착수(파일 충돌 회피).
3. 백로그(하): oracle DBM-009 숫자 IDLE_TIME 임계, pg idle_session_timeout, mysql DBM-006 int() 벤더버그, server 명령실패 출력.
4. **막힘(사용자 필요)**:
   - FW B′-2 (SECUI src_port): **ref/FW 실데이터가 이 환경에 없음** — 실데이터 환경에서만 가능.
   - B′-5 qwen3 헤더매핑 폴백 우선순위 상향 여부 — 사용자 결정 대기.
   - production 모델 `qwen3-coder:30b` 미설치 (`ollama pull qwen3-coder:30b` 필요) — LLM 품질 실검증 막힘.
   - push 여부 — 커밋은 로컬에 쌓는 중, push는 사용자 확인 후.

## 결정 사항 (이 세션)

- 판정 근거 표시 = xlsx 판단기준(standard) 채택(사용자 확정, 판단방법 method는 미포함).
- 도커 실증테스트 승인 없이 진행(사용자 지시, 메모리 저장됨).
- 8765 포트에 구버전 웹UI 서버가 떠 있어 시연은 8766 사용(기존 서버 안 죽임).
