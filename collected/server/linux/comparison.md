# 서버(Linux) 취약 주입 — 항목별 양호/취약 대조

| SRV | 항목명 | 상세설명 | 판단기준(요약) | 양호증거 | 취약증거 |
|---|---|---|---|---|---|
| SRV-006 | SMTP 서비스 로그 수준 설정 적절성 | o SMTP 서비스는 일반적으로 로그 설정을 할 때, 중요한 이벤트만 기 | * 양호 - SMTP 로그 수준 설정이 적절한 경우 * 취약 - SMTP 로그 수준 설정이 | -e [ smtp|sendmail|postfix|exim ][S] [ smtp|s | -e [ smtp|sendmail|postfix|exim ][S] [ smtp|s |
| SRV-011 | 시스템 관리자 계정의 FTP 사용 제한  | o FTP 서비스는 단독으로 사용 시 네트워크에서 계정과 패스워드가 암호 | * 양호 - ftpusers 파일이 존재하고, ftpusers 파일 안에 시스템 계정(ro | -e [ ftp ][S] [ ftp ][E] -=-=-=-=-=-=-=-=-=-= | -e [ ftp ][S] $ ps -ef | egrep vsftpd -e root |
| SRV-012 | .netrc 파일 내 중요 정보 미포함  | o .netrc 파일은 ftp 나 rexec 사용 시 자동 로그인을 위한 | * 양호 - .netrc 파일 내부에 아이디, 패스워드 등 민감한 정보가 없는 경우 * 취 | -e [ ftp ][S] [ ftp ][E] -=-=-=-=-=-=-=-=-=-= | -e [ ftp ][S] $ ps -ef | egrep vsftpd -e root |
| SRV-013 | Anonymous 계정의 FTP 서비스  | o FTP 서비스는 파일을 전송하기 위한 프로토콜을 기반으로 하는 서비스 | * 양호 - FTP 서비스 미사용 또는 Anonymous 설정 비활성화 * 취약 - Ano | -e [ ftp ][S] [ ftp ][E] -=-=-=-=-=-=-=-=-=-= | -e [ ftp ][S] $ ps -ef | egrep vsftpd -e root |
| SRV-021 | FTP 서비스 접근통제 설정 적절성 | o FTP에 대한 적절한 접근통제 정책 미적용 시 비인가자에게 시스템 파 | * 양호 - 특정 IP 주소 또는 호스트에서만 FTP 서버에 접속하도록 접근제어 설정을 적 | -e [ ftp ][S] [ ftp ][E] -=-=-=-=-=-=-=-=-=-= | -e [ ftp ][S] $ ps -ef | egrep vsftpd -e root |
| SRV-022 | 계정의 비밀번호 미설정, 빈 암호 사용  | o 시스템 접속 계정에 대한 비밀번호가 설정되어 있지 않은 경우 비인가자 | * 양호 - 계정들의 비밀번호가 모두 설정되어 있을 경우 * 취약 - 비밀번호가 설정되지  | $ cat /etc/passwd | awk 'BEGIN {FS=:} !=/bin/ | $ cat /etc/passwd | awk 'BEGIN {FS=:} !=/bin/ |
| SRV-026 | root 계정 원격 접속 제한 여부 | o 시스템 관리를 위한 root 계정의 원격 접속 허용은 악의적인 사용자 | * 양호 - root 계정의 원격 접속이 허용되지 않은 경우 * 취약 1. Telnet 사 | -e [ telnet ][S] [ telnet ][E] -=-=-=-=-=-=-= | -e [ telnet ][S] $ ps -ef | egrep telnetd -e  |
| SRV-027 | 서비스 접근 IP 및 포트 제한 여부 | o 서비스로의 접근이 통제되지 않을 경우 악의적인 사용자의 공격 목표가  | * 양호 - 시스템 서비스로의 접근통제가 적절하게 수행되고 있을 경우 (방화벽, tcp-w | $ firewall-cmd --list-all fsi.sh: 1500: firew | $ firewall-cmd --list-all fsi.sh: 1500: firew |
| SRV-028 | 원격 터미널 접속 타임아웃 설정 여부 | o 사용자 부재시, 비인가자에 의한 시스템 무단 사용을 방지하기 위해 일 | * 양호 - 세션 타임아웃 값이 900초 이하(15분)로 설정 되어 있을 경우 * 취약 - | -e [ telnet ][S] [ telnet ][E] -=-=-=-=-=-=-= | -e [ telnet ][S] $ ps -ef | egrep telnetd -e  |
| SRV-037 | 취약한 FTP 서비스 비활성화 | o FTP는 파일을 전송하기 위한 프로토콜로 계정과 패스워드를 암호화하지 | * 양호 - FTP 서비스가 비활성화 되어 있는 경우 * 취약 - FTP 서비스가 활성화  | -e [ ftp ][S] [ ftp ][E] | -e [ ftp ][S] $ ps -ef | egrep vsftpd -e root |
| SRV-069 | 비밀번호 관리정책 설정 적절성 | o 사용자 비밀번호에 대한 관리정책 설정이 미흡할 경우 유추하기 쉬운 비 | * 양호 - 비밀번호 관련 정책들이 설정되어 있을 경우 * 취약 - 비밀번호 관련 정책들이 | $ cat /etc/pam.d/common-password # # /etc/pam | $ cat /etc/pam.d/common-password # # /etc/pam |
| SRV-070 | 취약한 패스워드 저장 방식 사용 제한 여 | o 취약한 패스워드 저장 방식을 사용할 경우, 공격자에게 계정의 로그인  | * 양호 - shadow 패스워드를 사용하거나, 패스워드를 안전한 강도로 암호화하여 저장하 | $ cat /etc/passwd root:x:0:0:root:/root:/bin/ | $ cat /etc/passwd root:x:0:0:root:/root:/bin/ |
| SRV-073 | 관리자 그룹에 불필요한 사용자 제거 | o 시스템에 다수의 관리자 계정이 존재할 경우, 공격자가 탈취를 시도할  | * 양호 - 관리자 그룹에 불필요한 관리자 계정이 없을 경우 * 취약 - 관리자 그룹에 불 | $ cat /etc/group root:x:0: daemon:x:1: bin:x: | $ cat /etc/group root:x:0: daemon:x:1: bin:x: |
| SRV-074 | 불필요하거나 관리되지 않는 계정 제거 | o 시스템 설치 시 기본으로 생성되는 계정, 업무상 더 이상 사용되지 않 | * 양호 - 분기별 1회 이상 로그인 한 기록이 있고, 비밀번호를 변경하고 있는 경우 *  | $ awk -F":" '{print $1 " " $3}' /etc/shadow r | $ awk -F":" '{print $1 " " $3}' /etc/shadow r |
| SRV-083 | 시스템 스타트업 스크립트 권한 설정 적절 | o 시스템 스타트업 스크립트의 소유권 및 권한 설정이 미흡할 경우, 임의 | * 양호 - others에 쓰기 권한이 없을 경우 * 취약 - others에 쓰기 권한이  | $ grep -v "^#" /etc/inittab grep: /etc/initta | $ grep -v "^#" /etc/inittab grep: /etc/initta |
| SRV-091 | 불필요하게 SUID, SGID bit가  | o SUID(Set User-ID)와 SGID(Set Group-ID)가 | * 양호 - 불필요하게 SUID ,SGID가 설정된 파일이 없을 경우 * 취약 - 불필요하 | Manual inspection required ------------ $ nic | $ nice -n 5 find $HOMEDIR -type f -a -perm -4 |
| SRV-092 | 사용자 홈 디렉터리 경로 및 권한 설정  | o 사용자 계정별 홈 디렉터리 경로 및 권한이 올바로 설정되지 않을 경우 | * 양호 - 홈 디렉터리의 소유자와 실 사용자가 일치하고, 계정간 중복 홈 디렉터리가 존재 | -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=- | -=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=- |
| SRV-093 | 불필요한 world writable 파일 | o World Writable 파일 또는 디렉터리가 존재할 경우 이를 임 | * 양호 - 불필요한 world writable 파일이 존재하지 않는 경우 * 취약 - 불 | Manual inspection required | $ nice -n 5 find $HOMEDIR -perm -2 -type f -e |
| SRV-095 | 존재하지 않는 소유자 및 그룹 권한을 가 | o 사용하지 않는 디렉터리나 파일의 존재는 시스템 자원의 낭비 및 관리의 | * 양호 - 존재하지 않는 UID 및 GID를 가진 파일 및 디렉터리가 존재하지 않음 *  | Manual inspection required | $ nice -n 5 find $HOMEDIR -type f -a -nouser  |
| SRV-096 | 사용자 환경파일의 소유자 또는 권한 설정 | o 사용자 shell 환경 파일에 others 권한이 부여되어 있으면 사 | * 양호 - 사용자 환경 파일에 others에 부여된 권한이 없을 경우 * 취약 - 사용자 | $ ls -aldL /root/.bashrc -rw-r--r-- 1 root ro | $ ls -aldL /home/backdoor/.bash_logout -rw-r- |
| SRV-108 | 로그에 대한 접근통제 및 관리 적절성 | o 로그에 대한 접근통제가 미흡할 경우, 비인가자가 로그에서 정보를 획득 | * 양호 - 디렉터리 내 로그 파일들의 권한이 644 이하일 때 ※ 예시 : 디렉터리 내  | $ ls -aldL /var/log/* -rw-r--r-- 1 root root  | $ ls -aldL /var/log/* -rw-r--r-- 1 root root  |
| SRV-109 | 시스템 주요 이벤트 로그 설정 적절성 | o 시스템 문제 발생 시 원활한 원인 파악/문제 해결 등을 위해서 각종  | * 양호 - 1. syslog 로그 기록 정책이 내부 정책에 부합하게 설정되어 있는 경우  | $ cat /etc/pam.d/su # # The PAM configuration | $ cat /etc/pam.d/su # # The PAM configuration |
| SRV-112 | Cron 서비스 로깅 설정 적절성 | o Cron 서비스 실행에 대한 로깅 미설정 시 비인가자에 의한 시스템  | * 양호 - syslog 로그 기록 정책 또는 다른 로그 프로그램으로 cron 로그가 기록 | $ cat /etc/syslog.conf cat: /etc/syslog.conf: | $ cat /etc/syslog.conf cat: /etc/syslog.conf: |
| SRV-115 | 로그의 정기적 검토 및 보고 수행 여부 | o 각종 공격이나 시스템 오류에 대해 원활히 추적이 가능하려면, 로그에  | * 양호 - 로그 기록에 대해 정기적 검토, 분석, 보고서 작성 및 보고 등의 절차를 수행 | $ ls -aldL /var/log/* -rw-r--r-- 1 root root  | $ ls -aldL /var/log/* -rw-r--r-- 1 root root  |
| SRV-121 | root 계정의 PATH 환경변수 설정  | o 사용자가 특정 명령어를 실행할 때 PATH에 지정된 디렉터리를 검색하 | * 양호 - PATH 변수 내부에 ".", "::" 가 존재하지 않을 경우 * 취약 - P | $ echo $PATH /usr/local/sbin:/usr/local/bin:/ | $ echo $PATH /usr/local/sbin:/usr/local/bin:/ |
| SRV-131 | SU 명령 사용가능 그룹 제한 설정 적절 | o su(set user or superuser or switch use | * 양호 - /etc/pam.d/su 파일에 auth required pam_wheel.s | $ ls -alL /usr/bin/su -rwsr-xr-x 1 root root  | $ ls -alL /usr/bin/su -rwsr-xr-x 1 root root  |
| SRV-142 | 중복 UID가 부여된 계정 제한 여부 | o 시스템에 중복 UID가 존재하는 경우, 접근통제와 감사 추적의 어려움 | * 양호 - 동일한 UID로 설정된 사용자 계정이 존재하지 않는 경우 * 취약 - 동일한  | $ cat /etc/passwd root:x:0:0:root:/root:/bin/ | $ cat /etc/passwd root:x:0:0:root:/root:/bin/ |
| SRV-158 | 불필요한 Telnet 서비스 비활성화 | o Telnet 서비스는 Password 인증 방식 사용 시 데이터를 평 | * 양호 - Telnet 서비스가 비활성화 되어 있는 경우 * 취약 - Telnet 서비스 | -e [ telnet ][S] [ telnet ][E] | -e [ telnet ][S] $ ps -ef | egrep telnetd -e  |
| SRV-161 | ftpusers 파일의 소유자 및 권한  | o ftpusers 파일에서 FTP 서비스에 접근 가능한 사용자 계정을  | * 양호 - ftpusers 파일의 소유자가 root이고, 권한이 640 이하인 경우 ※  | -e [ ftp ][S] [ ftp ][E] -=-=-=-=-=-=-=-=-=-= | -e [ ftp ][S] $ ps -ef | egrep vsftpd -e root |
| SRV-163 | 시스템 사용 주의사항 출력 | o 비인가자의 부적절한 로그인 권한 획득을 사전에 방지하기 위하여 로그인 | * 양호 - /etc/issue.net, /etc/motd, /etc/ssh/sshd_co | $ cat /etc/motd cat: /etc/motd: No such file  | $ cat /etc/motd cat: /etc/motd: No such file  |
| SRV-164 | 구성원이 존재하지 않는 GID 제거 | o 계정이 존재하지 않는 그룹 권한(GID)이 존재할 경우, 해당 그룹이 | * 양호 - 구성원이 존재하지 않는 GID가 존재하지 않는 경우 * 취약 - 구성원이 존재 | $ cat /etc/passwd root:x:0:0:root:/root:/bin/ | $ cat /etc/passwd root:x:0:0:root:/root:/bin/ |
| SRV-165 | 불필요하게 Shell이 부여된 계정 제거 | o 로그인이 불필요한 계정들에 /bin/false 등을 부여하여 계정 탈 | * 양호 - 로그인이 필요하지 않은 계정에 /bin/false(nologin) 등이 부여된 | $ cat /etc/passwd root:x:0:0:root:/root:/bin/ | $ cat /etc/passwd root:x:0:0:root:/root:/bin/ |
| SRV-166 | 불필요한 숨김 파일 또는 디렉터리 제거 | o 악의적인 목적으로 생성한 파일 혹은 디렉터리를 숨김파일로 저장하는 경 | * 양호 - 불필요한 숨김 파일이 존재하지 않을 경우 * 취약 - 불필요한 숨김 파일이 존 | $ ls -alL /root total 16 drwx------ 2 root ro | $ ls -alL /home/backdoor total 20 drwxr-x---  |
| SRV-171 | FTP 서비스 정보 노출 방지 여부 | o FTP 접속 시 노출되는 배너에서 공격자가 유용한 정보 획득할 가능성 | * 양호 - FTP 접속 배너에 노출되는 정보가 없는 경우 * 취약 - FTP 접속 배너에 | -e [ ftp ][S] [ ftp ][E] -=-=-=-=-=-=-=-=-=-= | -e [ ftp ][S] $ ps -ef | egrep vsftpd -e root |