# server(OS) 항목 라벨 & LLM 지시 — 스캐폴드(구조 단계)
# label A: 기술 증거로 판정 가능 (LLM)
# label B: 담당자 인터뷰 필수 — verdict=판단보류 고정, LLM은 summary_instruction으로 증거 요약만
# label C: 기술적 한계 — 자동 판단보류, canned_message 출력 (LLM 호출 없음)
# label D: 외부지식 필요(EOL·패치 등) — 자동 판단보류, canned_message 출력 (LLM 호출 없음)
#
# 현재 상태: 항목 미등재 → 전 항목(106개 SRV) 기본 label A(LLM 판정).
# variant 키: aix / hpux / linux / solaris / win (variants: 오버라이드 가능).
#
# TODO(후속 과제 — 실수집 샘플 데이터 확보 후):
# - 평가기준 '서버' 시트의 판단기준/판단방법을 OS 변형별로 정독하여
#   C(기술한계)/D(EOL·패치 결정론)/B(인터뷰) 항목을 분류·등재한다.
# - 커널/패치 버전성 항목(예: OS 패치 적용 여부)은 D + eol_check/patch_check
#   후보로 보이나, 샘플 없이 추정 분류하지 않는다(여기 TODO로만 기록).
# - empty_means_good(빈 출력=양호) 후보도 스크립트 분석 후 profile.SERVER에 보강.
